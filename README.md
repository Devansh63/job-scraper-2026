just some code i generated to use for job scraping

pulls AI/ML internship and new grad listings from a supabase backend and writes them into markdown tables. runs on a cron via github actions.

needs SUPABASE_URL and SUPABASE_KEY set as repo secrets to work.
